name: gold_nwb
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip=23.0.1
  - pynwb=2.3.2
  - pip:
    - neuroconv[plexon,phy]==0.2.4
    - neo==0.12.0 # want 0.12.1, when abailable, to use less memory parsing .plx file headers
    - spikeinterface[full,widgets]==0.97.1
    - probeinterface==0.2.16
